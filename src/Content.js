import React from 'react'
import {Route, withRouter} from 'react-router-dom'
import {Col, Row, Container} from 'react-bootstrap'

function Content() {
    return (
        <div style={{border:'solid blue', height:'500px', width:'100%'}}>
            
        </div>
    )
}

export default withRouter(Content)